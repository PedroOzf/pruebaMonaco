export class Comentario{
  constructor(
    idComentario: number,
    texto: string,
    likes:number,
    dislikes:number,
    idUsuario: number,
    idProblema: number,
    fecha: number
  ) {
  }
}
